const SCOUT=['find','locate','search','references','callers','summarize','where is']
export function deterministicRoute({task='',risk='normal',rootCause=null,attempts=0}){const text=task.toLowerCase();if(attempts>=2)return{role:'exceptional',action:'escalate',reason:'two-strike-limit'};if(SCOUT.some(term=>text.includes(term)))return{role:'scout',action:'delegate',reason:'retrieval-or-mechanical-task'};if(risk==='high'&&!rootCause)return{role:'deep_debugger',action:'delegate',reason:'high-risk-root-cause-unknown'};return{role:'engineer',action:'delegate',reason:rootCause?'root-cause-established':'default-medium-engineering'}}
