/** Jev is optional. Deterministic routing remains authoritative until the first-party contract is confirmed. */
export function jevConfigured(env=process.env){return Boolean(env.TYPESAFE_API_KEY&&env.TYPESAFE_BASE_URL)}
export async function askJev(decision,options={}){const env=options.env||process.env;if(!jevConfigured(env))return{available:false,decision:null,reason:'jev-not-configured'};const fetchImpl=options.fetchImpl||globalThis.fetch;if(!fetchImpl)throw new Error('fetch is unavailable');const endpoint=new URL('/v1/systemone',env.TYPESAFE_BASE_URL).toString();const response=await fetchImpl(endpoint,{method:'POST',headers:{'content-type':'application/json',authorization:`Bearer ${env.TYPESAFE_API_KEY}`},body:JSON.stringify({model:env.TYPESAFE_MODEL||'jev',input:decision.input,choices:decision.choices})});if(!response.ok)return{available:false,decision:null,reason:`jev-http-${response.status}`};return{available:true,decision:await response.json(),reason:'jev'}}
