import {planTask} from '../orchestrator.js';import {resolveRoleTarget} from '../execution/roleResolver.js';import {executeDelegation} from '../execution/executor.js'
export async function executeRoutedTask(input={},options={}){const env=options.env||process.env;const plan=await planTask(input,options);if(!plan.delegation)return{plan,execution:null};const target=resolveRoleTarget(plan.route.role,env);if(!target.configured)return{plan,execution:null,target,reason:'provider-model-not-configured'};return{plan,target,execution:await executeDelegation({delegation:plan.delegation,...target,budget:input.budget,state:input.executionState||{},env})}}
