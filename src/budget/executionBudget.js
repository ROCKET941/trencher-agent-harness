const i=(v,d)=>Number.isInteger(Number(v))&&Number(v)>0?Number(v):d
export function resolveBudget(x={},e=process.env){return{maxInputTokens:i(x.maxInputTokens??e.HARNESS_MAX_INPUT_TOKENS,12000),maxOutputTokens:i(x.maxOutputTokens??e.HARNESS_MAX_OUTPUT_TOKENS,5000),maxDelegations:i(x.maxDelegations??e.HARNESS_MAX_DELEGATIONS,2),maxParallel:i(x.maxParallel??e.HARNESS_MAX_PARALLEL,1),maxAttempts:i(x.maxAttempts??e.HARNESS_MAX_ATTEMPTS,2)}}
export function checkExecutionBudget(s={},b=resolveBudget()){const r=[];if((s.inputTokens||0)>=b.maxInputTokens)r.push('input-token-budget');if((s.outputTokens||0)>=b.maxOutputTokens)r.push('output-token-budget');if((s.delegations||0)>=b.maxDelegations)r.push('delegation-budget');if((s.active||0)>=b.maxParallel)r.push('parallel-budget');if((s.attempts||0)>=b.maxAttempts)r.push('attempt-budget');return{allowed:!r.length,reasons:r}}
