import {getProvider} from '../providers/registry-v12.js';import {resolveBudget,checkExecutionBudget} from '../budget/executionBudget.js'
export async function executeDelegation({delegation,provider,model,budget:asked,state={}}){const budget=resolveBudget(asked),gate=checkExecutionBudget(state,budget);if(!gate.allowed)return{executed:false,reason:'budget-blocked',gate,state};const result=await getProvider(provider).execute({role:delegation.role,task:delegation.context?.task||'',context:delegation.context||{},instruction:delegation.instruction||'',provider,model,budget});return{executed:true,result,state:{...state,active:0,inputTokens:(state.inputTokens||0)+result.usage.inputTokens,outputTokens:(state.outputTokens||0)+result.usage.outputTokens,delegations:(state.delegations||0)+1}}}
