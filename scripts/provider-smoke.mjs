import {ProviderAdapter,normalizeResult} from '../src/providers/base.js';import {registerProvider,clearProviders} from '../src/providers/registry-v12.js';import {executeDelegation} from '../src/execution/executor.js';class Mock extends ProviderAdapter{constructor(){super('mock')}async execute(q){return normalizeResult({provider:'mock',model:'mock',role:q.role,output:'ok',usage:{inputTokens:10,outputTokens:5}})}}clearProviders();registerProvider('mock',new Mock());const r=await executeDelegation({delegation:{role:'engineer',context:{task:'bounded test'}},provider:'mock',model:'mock'});if(!r.executed||r.result.output!=='ok')throw new Error('provider smoke failed');console.log(JSON.stringify(r,null,2))
